{
  "name": "tworiders",
  "version": "1.0.0",
  "private": true,
  "description": "tworiders.com — a bicycle travel journal, built with Hugo and deployed to Cloudflare Workers.",
  "homepage": "https://www.tworiders.com",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "hugo server --bind 0.0.0.0",
    "build": "hugo --gc --minify",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "hugo-extended": "0.147.9",
    "wrangler": "^4.20.0"
  }
}
